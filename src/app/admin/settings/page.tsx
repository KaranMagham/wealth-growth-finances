export default function AdminSettingsPage() { const settings = [{ label: "Application", value: "Wealth Growth" }, { label: "Admin authorization", value: "ADMIN_USER_ID configured server-side" }, { label: "AI configuration", value: "Secrets are not exposed in the browser" }, { label: "Maintenance mode", value: "Not configured" }]; return <section className="space-y-6"><header><p className="text-sm font-semibold uppercase tracking-[0.25em] text-[#10B981]">Operations</p><h1 className="mt-2 text-3xl font-semibold">Admin settings</h1><p className="mt-2 text-[#7894AA]">Safe configuration visibility without exposing environment secrets.</p></header><div className="grid gap-4 sm:grid-cols-2">{settings.map((setting) => <div key={setting.label} className="rounded-2xl border border-[#1E3347] bg-[#0D1D2E] p-5"><p className="text-sm text-[#7894AA]">{setting.label}</p><p className="mt-3 font-medium text-white">{setting.value}</p></div>)}</div></section>; }
